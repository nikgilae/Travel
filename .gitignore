# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.claude
CLAUDE.md

# Virtual environments
.venv
.env
.env.*
!.env.example
.kilo
.gstack/

# Database dumps (may contain real user data — never commit)
*.sql

# RAG eval: регенерируемые сырые данные (не deliverable — пересчитываются
# scripts/export_poi_corpus.py и scripts/evaluate_rag_retrieval.py)
backend/eval/rag/corpus/
backend/eval/rag/*-results.json

.prd


# Содержит боевые секреты (POSTGRES_PASSWORD, TG_BOT_TOKEN)
DEPLOY_HANDOFF.md

# Зависимости ставятся из package-lock.json, в репозитории им не место
node_modules/
